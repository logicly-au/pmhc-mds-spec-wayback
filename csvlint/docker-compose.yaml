version: "2.2"

services:
  csvlint:
    build:
      context: ../
      dockerfile: ./csvlint/Dockerfile
    image: docker.sdlocal.net/pmhcwayback/csvlint:master
    volumes:
      - ../data:/spec/data

